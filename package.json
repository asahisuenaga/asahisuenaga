{
"scripts": {
  "build": "mkdir -p dist && cp index.html robots.txt sitemap.xml dist/ && cp -r icons images locales sounds dist/ && npx terser script.js -o dist/script.js && npx csso styles.css -o dist/styles.css"
},
  "devDependencies": {
    "terser": "latest",
    "csso-cli": "latest",
    "wrangler": "latest"
  }
}