{
"scripts": {
  "build": "mkdir -p dist && cp index.html dist/ && npx terser script.js -o dist/script.js && npx csso style.css -o dist/style.css"
},
  "devDependencies": {
    "terser": "latest",
    "csso-cli": "latest",
    "wrangler": "latest"
  }
}