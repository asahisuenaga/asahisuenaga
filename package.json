{
"scripts": {
  "build": "mkdir -p dist && cp index.html dist/ && npx terser script.js -o dist/script.js && npx csso styles.css -o dist/styles.css"
},
  "devDependencies": {
    "terser": "latest",
    "csso-cli": "latest",
    "wrangler": "latest"
  }
}