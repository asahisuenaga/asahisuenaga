{
  "devDependencies": {
    "terser": "latest",
    "csso-cli": "latest",
    "html-minifier": "latest",
    "wrangler": "latest"
  },
  "scripts": {
    "build": "mkdir -p dist && terser js/*.js -o dist/script.js && csso css/*.css -o dist/style.css && html-minifier --collapse-whitespace index.html -o dist/index.html"
  }
}